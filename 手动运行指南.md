# Jekyll 本地运行指南

本指南将帮助你手动在本地运行 Jekyll 网站，无需使用自动化脚本。

---

## 📋 前置要求

在开始之前，请确保你的电脑已安装以下软件：

### 1. Ruby（必需）

**Windows 用户：**
- 访问 [RubyInstaller](https://rubyinstaller.org/)
- 下载并安装 **Ruby+Devkit** 版本（推荐最新稳定版）
- 安装时勾选 "Add Ruby executables to your PATH"
- 安装完成后，打开新的命令行窗口验证：
  ```bash
  ruby -v
  ```
  应该显示 Ruby 版本号，例如：`ruby 3.2.0`

**macOS 用户：**
- 使用 Homebrew 安装：
  ```bash
  brew install ruby
  ```
- 或使用系统自带的 Ruby（macOS 通常已预装）

**Linux 用户：**
- Ubuntu/Debian：
  ```bash
  sudo apt-get update
  sudo apt-get install ruby-full build-essential
  ```
- CentOS/RHEL：
  ```bash
  sudo yum install ruby ruby-devel
  ```

### 2. Bundler（Ruby 包管理器）

Bundler 通常随 Ruby 一起安装，如果没有，请手动安装：

```bash
gem install bundler
```

验证安装：
```bash
bundle -v
```

---

## 🚀 运行步骤

### 步骤 1：打开项目目录

在命令行中，切换到项目根目录：

```bash
cd E:\github\JNU2304-main
```

> **提示**：将路径替换为你实际的项目路径

**如果 `cd` 命令没有切换目录，请尝试以下方法：**

1. **检查路径是否正确**：
   ```bash
   dir E:\github\JNU2304-main
   ```
   如果提示"找不到路径"，说明路径不存在或拼写错误。

2. **使用引号包裹路径**（如果路径中有空格或特殊字符）：
   ```bash
   cd "E:\github\JNU2304-main"
   ```

3. **先切换到 E 盘，再进入目录**：
   ```bash
   E:
   cd github\JNU2304-main
   ```

4. **使用 `/d` 参数（CMD 中跨盘符切换）**：
   ```bash
   cd /d E:\github\JNU2304-main
   ```
   > **重要**：在 CMD 中，如果当前在 C 盘，直接 `cd` 到 E 盘可能不会切换。使用 `cd /d` 可以强制切换盘符。

5. **验证是否切换成功**：
   ```bash
   cd
   ```
   或
   ```bash
   echo %CD%
   ```
   应该显示 `E:\github\JNU2304-main`

**推荐方法**（最简单）：
```bash
cd /d E:\github\JNU2304-main
```

### 步骤 2：安装依赖

首次运行或更新依赖时，需要安装项目所需的 gem 包：

```bash
bundle install
```

**这个过程可能需要几分钟**，因为需要从网络下载所有依赖包。你会看到类似以下的输出：

```
Fetching gem metadata from https://rubygems.org/.........
Resolving dependencies...
Using public_suffix 4.0.7
Using addressable 2.8.1
...
Bundle complete! X Gemfile dependencies, Y gems now installed.
```

**常见问题：**
- 如果下载很慢，可能是网络问题，请耐心等待
- 如果出现 SSL 证书错误，可以尝试：
  ```bash
  gem sources --add https://gems.ruby-china.com/ --remove https://rubygems.org/
  bundle install
  ```
- 如果出现权限错误（Linux/macOS），可能需要使用 `sudo`：
  ```bash
  sudo bundle install
  ```

### 步骤 3：检查依赖状态（可选）

如果你想检查依赖是否都已安装：

```bash
bundle check
```

如果显示 "The Gemfile's dependencies are satisfied"，说明依赖已就绪。

### 步骤 4：启动 Jekyll 服务器

运行以下命令启动本地服务器：

```bash
bundle exec jekyll serve
```

或者使用简写：

```bash
bundle exec jekyll s
```

**服务器启动后，你会看到类似输出：**

```
Configuration file: E:/github/JNU2304-main/_config.yml
            Source: E:/github/JNU2304-main
       Destination: E:/github/JNU2304-main/_site
 Incremental build: disabled. Enable with --incremental
      Generating...
                    done in X.XXX seconds.
 Auto-regeneration: enabled for 'E:/github/JNU2304-main'
    Server address: http://127.0.0.1:4000/
  Server running... press ctrl-c to stop.
```

### 步骤 5：访问网站

打开浏览器，访问：

```
http://127.0.0.1:4000/
```

或

```
http://localhost:4000/
```

现在你应该能看到网站了！

---

## 🔧 常用命令

### 启动服务器（带实时预览）

```bash
bundle exec jekyll serve
```

### 启动服务器（增量构建，更快）

```bash
bundle exec jekyll serve --incremental
```

或简写：

```bash
bundle exec jekyll serve -I
```

### 启动服务器（指定端口）

如果 4000 端口被占用，可以指定其他端口：

```bash
bundle exec jekyll serve --port 4001
```

### 仅构建网站（不启动服务器）

```bash
bundle exec jekyll build
```

构建后的文件会输出到 `_site` 目录。

### 清理构建文件

```bash
bundle exec jekyll clean
```

这会删除 `_site` 目录和 `.jekyll-cache` 目录。

---

## ❓ 常见问题

### 0. `cd` 命令无法切换目录（跨盘符问题）

**问题现象**：在 CMD 中执行 `cd E:\github\JNU2304-main` 后，提示符仍然显示 `C:\Users\...`，目录没有切换。

**原因**：在 Windows CMD 中，如果当前在 C 盘，直接使用 `cd` 切换到其他盘符（如 E 盘）时，CMD 不会自动切换盘符。

**解决方法**：

**方法 1（推荐）**：使用 `cd /d` 参数强制切换盘符和目录
```bash
cd /d E:\github\JNU2304-main
```

**方法 2**：先切换盘符，再切换目录
```bash
E:
cd github\JNU2304-main
```

**方法 3**：使用 PowerShell（自动支持跨盘符切换）
```powershell
cd E:\github\JNU2304-main
```

**验证是否切换成功**：
```bash
cd
```
或
```bash
echo %CD%
```
应该显示 `E:\github\JNU2304-main`

### 1. 提示 "command not found: bundle" 或 "bundle 不是内部或外部命令"

**原因**：Bundler 未安装或未添加到 PATH

**解决方法**：
```bash
gem install bundler
```

如果还是不行，检查 Ruby 是否正确安装：
```bash
ruby -v
gem -v
```

### 1.5. 提示 "bundler: command not found: jekyll" 或 "Install missing gem executables with `bundle install`"

**问题现象**：运行 `bundle exec jekyll serve` 时提示找不到 jekyll 命令。

**原因**：项目依赖还没有安装。每个 Jekyll 项目都需要单独运行 `bundle install` 来安装依赖，即使你在其他项目中已经安装过 Jekyll。

**解决方法**：
```bash
bundle install
```

这会根据 `Gemfile` 安装所有必需的依赖（包括 Jekyll），可能需要 2-5 分钟。

**为什么其他项目（如 `lyzbcy.github.io`）已经安装好了？**
- 每个 Jekyll 项目都有独立的依赖环境
- 你在那个项目中已经运行过 `bundle install`
- 当前项目（`JNU2304-main`）还没有运行过，所以需要先安装

**验证安装是否成功**：
安装完成后，应该会生成 `Gemfile.lock` 文件。然后就可以正常运行：
```bash
bundle exec jekyll serve
```

### 2. 提示 "Could not locate Gemfile"

**原因**：当前目录不是项目根目录

**解决方法**：确保在项目根目录（包含 `Gemfile` 的目录）运行命令

### 3. 端口 4000 已被占用

**解决方法**：使用其他端口
```bash
bundle exec jekyll serve --port 4001
```

### 4. 修改文件后网站没有更新

**解决方法**：
- 检查服务器是否正在运行
- 尝试停止服务器（Ctrl+C）后重新启动
- 使用 `--incremental` 参数可能更快

### 5. 依赖安装失败

**可能原因**：
- 网络连接问题
- Ruby 版本不兼容
- 缺少编译工具（Windows 需要 Devkit）

**解决方法**：
- 检查网络连接
- 确保安装了 Ruby+Devkit（Windows）
- 尝试更新 RubyGems：
  ```bash
  gem update --system
  ```
- 清理并重新安装：
  ```bash
  bundle clean --force
  bundle install
  ```

### 6. 中文显示乱码

**Windows 用户**：在运行命令前设置编码：
```bash
chcp 65001
bundle exec jekyll serve
```

或在 PowerShell 中：
```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
bundle exec jekyll serve
```

---

## 📝 完整流程示例

以下是一个完整的首次运行流程：

```bash
# 1. 进入项目目录（使用 /d 参数确保跨盘符切换成功）
cd /d E:\github\JNU2304-main

# 2. 检查 Ruby 是否安装
ruby -v

# 3. 检查 Bundler 是否安装
bundle -v

# 4. 如果没有 Bundler，先安装
gem install bundler

# 5. 安装项目依赖（首次运行需要）
bundle install

# 6. 启动服务器
bundle exec jekyll serve

# 7. 在浏览器中打开 http://localhost:4000/
```

---

## 🛑 停止服务器

当你想停止服务器时，在运行服务器的命令行窗口中按：

```
Ctrl + C
```

---

## 💡 提示

1. **首次运行较慢**：第一次运行 `bundle install` 需要下载所有依赖，可能需要 2-5 分钟，请耐心等待。

2. **保持服务器运行**：修改网站文件后，Jekyll 会自动重新构建，刷新浏览器即可看到更改。

3. **增量构建**：使用 `--incremental` 参数可以加快构建速度，只重新构建更改的文件。

4. **查看帮助**：运行 `bundle exec jekyll help` 可以查看所有可用命令。

---

## 🔧 Git 提交信息配置（中文）

为了确保提交更改时使用中文，项目已包含 Git 提交信息模板（`.gitmessage`）。

### 配置 Git 使用中文提交模板

在项目根目录执行以下命令：

```bash
git config commit.template .gitmessage
```

或者全局配置（所有项目都使用中文模板）：

```bash
git config --global commit.template .gitmessage
```

### 提交信息模板说明

提交信息应遵循以下格式：

```
<类型>: <简短描述>

<详细描述（可选）>
```

**类型说明：**
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整（不影响功能）
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具链相关
- `ci`: 持续集成相关

**示例：**
```
feat: 添加日历事件点击跳转功能

- 支持事件卡片点击跳转到指定链接
- 优化模态框交互，点击内容区域不会关闭
- 添加事件链接的视觉提示（箭头图标）
```

### 使用 AI 工具生成提交信息

项目已包含 `.cursorrules` 配置文件，用于指导 Cursor AI 使用中文生成提交信息。

**如果 AI 仍然生成英文提交信息，请：**
1. 确保 `.cursorrules` 文件存在于项目根目录
2. 在提交时明确提醒 AI："请使用中文生成提交信息，参考 .cursorrules 文件"
3. 或者手动将英文提交信息翻译为中文

**提交信息要求：**
- ✅ 使用中文描述所有更改
- ✅ 遵循上述提交信息格式
- ✅ 参考 `.gitmessage` 模板文件
- ❌ 禁止使用英文生成提交信息

---

## 📚 更多资源

- [Jekyll 官方文档](https://jekyllrb.com/docs/)
- [Chirpy 主题文档](https://github.com/cotes2020/jekyll-theme-chirpy/wiki)
- [Ruby 官方文档](https://www.ruby-lang.org/zh_cn/documentation/)

---

**祝你使用愉快！** 🎉

